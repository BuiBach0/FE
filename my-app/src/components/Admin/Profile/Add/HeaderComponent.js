import React from 'react';
import { Layout } from 'antd';

const HeaderComponent = () => (
  <Layout.Header style={{ background: '#2C3E50', color: '#fff', textAlign: 'center', padding: 0 }}>
    <h1>Profile Information</h1>
  </Layout.Header>
);

export default HeaderComponent;
